const http = require ('http')
const server = http.createServer((req, res)=>{
})

//const/let nombre = (()=>{ codigo });

const PORT = 127.0.0.1:5500/paginagustos/indexedDB.html;

server.listen(PORT, () => {
    console.log(`Servidor escuchando el puerto ${PORT}`);
});